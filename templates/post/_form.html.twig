{{ form_start(form, { 'attr': { 'enctype': 'multipart/form-data', 'files' :  'true' } }) }}
{{ form_widget(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
